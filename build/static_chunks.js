
/**
 * static chunks are used to generate CDN-speed-up html file
 */
const path = require('path');
const fs = require('fs');

const getAllStaticChunks = (options = {}) => {
  const { staticEntryDir } = options;
}

module.exports = exports = getAllStaticChunks;
